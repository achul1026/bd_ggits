<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN' 'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>

<mapper namespace='com.neighbor21.ggits.common.mapper.AdsiDsrcColctInfoCurMapper'>
	<sql id="dsrcInfo-Where">
		<if test="(strDt != '' and strDt != '') and (endDt != '' and endDt != null)">
			AND ADCIC.CLCT_DT 
			BETWEEN TO_TIMESTAMP(#{strDt}, 'YYYY-MM-DD HH24:MI:SS') 
		    	AND TO_TIMESTAMP(#{endDt}, 'YYYY-MM-DD HH24:MI:SS')
		</if>
		<if test="(strDt != '' and strDt != null) and (endDt == '' or endDt == null)">
			<![CDATA[
			AND TO_TIMESTAMP(#{strDt}, 'YYYY-MM-DD HH24:MI:SS')  <= ADCIC.CLCT_DT
			]]>
		</if>
		<if test="(strDt == '' or strDt == null) and (endDt != '' and endDt != null)">
			<![CDATA[
			AND ADCIC.CLCT_DT <= TO_TIMESTAMP(#{endDt}, 'YYYY-MM-DD HH24:MI:SS')
			]]>
		</if>
		<if test="searchContent != null and searchContent != ''">
			AND (AMFD1.RSE_NM LIKE '%' ||  #{searchContent} || '%') 
				OR (AMFD2.RSE_NM LIKE '%' ||  #{searchContent} || '%') 
				OR (CGSLAM.ROAD_NAME LIKE '%' || #{searchContent} || '%')
		</if>
		<if test="(sigunCdId != null and sigunCdId != '') and sigunCdId != 'searchAllLocation'">
			AND CGSLAM.ADSTDG_CD LIKE SUBSTRING(#{sigunCdId}, 0, 5) || '%'
		</if>
	</sql>
	
	<select id="findAdsiDsrcColctInfoList" parameterType="adsiDsrcColctInfoCur" resultType="adsiDsrcColctInfoCur">
		SELECT 
			AMFD1.RSE_NM as STR_RSE_NM,
			AMFD2.RSE_NM as END_RSE_NM,
			CGSLAM.ROAD_NAME,
			ROUND(SUM(ADCIC.SPEED) / COUNT(AMFD1.RSE_NM), 2) AS AVG_SPD
		FROM ADSI_DSRC_COLCT_INFO_CUR ADCIC
		INNER JOIN ADSI_DSRC_SCTN_INFO ADSI
			ON	ADCIC.DSRC_SCTN_ID = ADSI.DSRC_SCTN_ID 
		INNER JOIN ADSI_M_FA_DSRC AMFD1 
			ON	AMFD1.RSE_ID = ADSI.START_RSE_ID
		INNER JOIN ADSI_M_FA_DSRC AMFD2
			ON	AMFD2.RSE_ID = ADSI.END_RSE_ID
		INNER JOIN ADSI_DSRC_SCTN_STLINK_MPPG ADSSM
			ON ADCIC.DSRC_SCTN_ID = ADSSM.DSRC_SCTN_ID 
		INNER JOIN C_GM_STD_LINK_ADSTDG_MPPG CGSLAM 
			ON ADSSM.STD_LINK_ID = CGSLAM.LINK_ID 
		WHERE 1=1
		<include refid="dsrcInfo-Where"/>
		GROUP BY AMFD1.RSE_NM, AMFD2.RSE_NM, CGSLAM.ROAD_NAME
		ORDER BY CGSLAM.ROAD_NAME
		<if test="page != null and page != '' and page != 0">
			LIMIT 10 OFFSET (#{page} - 1) * 10	
	    </if>
	</select>
	
	<select id="countDsrcColctInfo" parameterType="adsiDsrcColctInfoCur" resultType="int">
		SELECT 
			COUNT(A.STR_RSE_NM)
		FROM (
				SELECT 
					AMFD1.RSE_NM as STR_RSE_NM
				FROM ADSI_DSRC_COLCT_INFO_CUR ADCIC
				INNER JOIN ADSI_DSRC_SCTN_INFO ADSI
					ON	ADCIC.DSRC_SCTN_ID = ADSI.DSRC_SCTN_ID 
				INNER JOIN ADSI_M_FA_DSRC AMFD1 
					ON	AMFD1.RSE_ID = ADSI.START_RSE_ID
				INNER JOIN ADSI_M_FA_DSRC AMFD2
					ON	AMFD2.RSE_ID = ADSI.END_RSE_ID
				INNER JOIN ADSI_DSRC_SCTN_STLINK_MPPG ADSSM
					ON ADCIC.DSRC_SCTN_ID = ADSSM.DSRC_SCTN_ID 
				INNER JOIN C_GM_STD_LINK_ADSTDG_MPPG CGSLAM 
					ON ADSSM.STD_LINK_ID = CGSLAM.LINK_ID 
				WHERE 1=1
				<include refid="dsrcInfo-Where"/>
				GROUP BY AMFD1.RSE_NM, AMFD2.RSE_NM, CGSLAM.ROAD_NAME
		)A
	</select>
</mapper>
